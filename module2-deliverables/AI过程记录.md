# 模块二 AI 过程记录（可审查版）

## 任务输入与提示词

用户要求沿用模块一 PEFT，采用方案 2“AI 测”，独立于模块一后续四个提交工作：生成不少于 15 条新用例及逐条自动化测试，验证至少一个新缺陷，修复并回归，按课程模板交付报告；不制作 PPT 或视频。本轮实施时给 AI 的具体约束还包括：不得重复模块一 TC-001～TC-036；必须先留修复前失败证据；不得把未验证的异常写成缺陷；不得伪造组员独立提交或贡献。

## AI 候选建议（未审查的草案）

1. 首任务前空隙刚好等于任务时长。
2. 小数时长恰好填满首任务前空隙。
3. 非零 `time_offset` 下恰好填满首空隙。
4. 首空隙大于任务时长。
5. 首空隙小于任务时长。
6. 两已有任务之间的空隙恰好填满。
7. `time_offset` 落在已有任务间空隙中。
8. 插入新任务后原有事件仍在调度表中。
9. 插入后字典输出中的同处理器前驱正确。
10. 调度后调用者的 DAG 节点/边属性不变。
11. 非方阵通信矩阵拒绝。
12. 含无穷大通信矩阵拒绝。
13. 负通信值拒绝。
14. 零处理器拒绝。
15. 多根 DAG 拒绝。
16. 多终点 DAG 拒绝。
17. 禁止重标号时非连续编号拒绝。
18. 无向图拒绝。
19. 一维计算矩阵拒绝。
20. 元组输入可正规化。
21. 经典样例总工期正确。
22. 循环 DAG 拒绝。
23. 负执行时间拒绝。

## 对 AI 建议的核查与修正

- 21～23 已由模块一 TC-009、TC-025、TC-026 覆盖，因此排除，不计模块二新用例。
- 1～3、9 从不同数值与输出角度验证同一个潜在缺陷；它们不是四个独立缺陷。
- 4～7 是同一插入逻辑的相邻边界与对照组，便于排除“所有空隙都失效”的错误解释。
- 8、10 检查副作用；11～19 检查输入契约；20 检查正常化路径。每条对应 `tests_module2/test_module2.py` 的同号函数。
- 上述核查由本次 AI 助手实施，尚不能冒充两名学生各自的人工核查。组员若有进一步复核，应另行记录姓名、时间与结论。

## 修复前运行

基线 `d38620b`、模块二测试代码尚未修复时，20 条中 16 通过、4 失败（M2-TC-001、002、003、009）；原始结果见 `evidence/baseline-fail.xml`。首个失败的实际排程为新任务 `[4,6]`，预期为 `[0,2]`。根因候选为 `peft/peft.py::_compute_eft` 对首空隙采用严格 `> 0`，而内部空隙采用 `>= 0`。完成最小修复与回归后再填写最终结论。

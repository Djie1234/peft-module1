# 模块二小组协作说明

模块二由颜子轩、余璨合作完成，两人贡献各占 50%。本项目采用 AI 辅助测试：AI 用于候选生成与脚本草案，小组依据代码、测试输入和 JUnit 结果共同确认用例预期、缺陷结论和回归结果。

## 颜子轩（Djie1234，50%）

- 维护模块二自动化测试、最小缺陷修复与一键运行入口。
- 保存修复前失败、修复后通过及全量回归的原始 JUnit XML。
- 整理测试工程、执行证据及项目运行说明。

## 余璨（50%）

- 复核 Excel 中 20 条用例的输入、预期和实际结果，重点审查 M2-TC-001～009 的空隙与偏移边界。
- 分析已有事件 `[2,4]`、新任务耗时 `2` 的首空隙反例，确认基线排程 `[4,6]` 与修复后 `[0,2]` 的差异。
- 审查 AI 候选排重、四条失败的根因归并，并整理测试报告与缺陷报告。

## 共同结论

小组确认：23 条 AI 候选中 3 条与模块一既有用例重复，最终保留 20 条模块二用例；修复前 4 条失败归并为 DEF-M2-001 一项缺陷。首空隙条件由 `> 0` 调整为 `>= 0` 后，模块二 20/20 通过，全量 58/58 通过。
